%header.navbar.navbar-inverse.navbar-fixed-top
  %nav.navbar-inner
    .container(ng-controller='DashboardCtrl')
      %a.brand( href='/' )
        %img( alt='Bluefull' size='18x24' src='/assets/bluefull.png' )
        SpokenVote

      %ul#main-nav.nav
        %hidden_field_tag( id='hub_filter' ui-select2='hubFilterSelect2' ng-model='hubFilter' )

      %ul.nav.pull-right
        %li.dropdown
          %ul( type='button' class='dropdown-toggle' bs-dropdown='help_dropdown' ) <b class="white"> Help </b> <b class="caret"></b>

        %ng-include(src="'/assets/shared/_user_nav.html.haml'")

        %li( ng-show='!currentUser.username')
          %span
            %button( ng-click="signIn()" class='btn btn-info btn-bold' ) Sign In
          %span
            %button( ng-click="register()" class='btn btn-primary btn-bold' ) Join Us!


      -#%ul.nav.pull-right
        %li.dropdown
          %span( class='dropdown-toggle' ) Help <b class="caret"></b>
          .dropdown-menu(  bs-dropdown='help_dropdown' )
            -#= link_to raw('Help <b class="caret"></b>'), '#', remote: true, class: 'dropdown-toggle', data: {toggle: 'dropdown'}
            -#%ul.dropdown-menu( type='button' class='btn' bs-dropdown='help_dropdown' ) Help
            -#%li( class='dropdown-toggle' )
            -#%span( class='caret' )

            -#%li= link_to 'About this Site', about_path
            -#%li= link_to 'Developers', 'http://railsforcharity.github.com/spokenvote'
            -#%li= link_to 'Help', help_path
        -#- if current_user && user_signed_in?
          = render partial: 'shared/user_nav'
        -#- else

