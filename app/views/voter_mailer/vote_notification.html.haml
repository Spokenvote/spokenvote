!!!
%html
  %head
    %meta( charset="utf-8" )
    = stylesheet_link_tag 'mailer', media: 'all'

  %body
    .sv-logo( href='/landing' )
      %span.spoken
        Spoken
      %span.vote
        Vote

    %h1
      %p= "Hello #{@recipient.username}"
    %br
    %p
      A new vote has been created. Please check it out. (html version)
  - @votes.each do |vote|
    %p= "#{vote.user.name} voted on the proposal '#{vote.proposal.statement}'"

  -#%div{:style => "font-family:arial,verdana,sans-serif;font-size:12px; margin: 5px;"}
    %h1
      %p= "Hello #{@recipient.username}"
    %br
    %p
      A new vote has been created. Please check it out. (html version)
  -#- @votes.each do |vote|
  -#  %p= "#{vote.user.name} voted on the proposal '#{vote.proposal.statement}'"


/ @votes[0].name

-#- taskp = TaskPresenter.new(@task, self)